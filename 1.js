var a = 100
var a = 200
var c = 200

// 在master分支里面修改的代码

// 当我们修改了文件之后 它就不让切换分支
// git add .
// git commit -m "消息"

// git push -u 绑定默认分支
// git push  当绑定了默认分支后 直接就可以使用push

// 例如

// git push -u github master
// git push  ===  git push -u github master